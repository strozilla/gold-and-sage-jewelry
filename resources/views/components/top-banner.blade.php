@props(['heading' => 'Unlock Your Wardrobes Potential', 'subheading' => 'Discover the perfect pieces for your wardrobe.'])

<section class="bg-primary-dark text-cream py-6 text-center">
  <div class="container mx-auto">
    <h2 class="text-3xl">{{ $heading }}</h2>
    <p class="text-lg mt-2">{{ $subheading }}</p>
  </div>
</section>
